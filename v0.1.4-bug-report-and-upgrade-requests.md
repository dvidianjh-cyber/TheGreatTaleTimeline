# v0.1.4-bug-report-and-upgrade-requests

## Main Bugs:

5. Theme change incomplete - you changed the header and flyout to parchment - it looks great. but the canvas/lanes are still dark and the text hard to read. I see that you made the lanes 'lighter', but I still don't want the black canvas background. please change the canvas background to the same parchment style background with dark text, and add some 3d effects to the header pane and the flyout to simulate that they are 'on top' of the canvas

6. 'Parent Event' (long time scale) Hover card - showing the wrong date range when zoomed out - e.g. the "Forging of the Rings" event shows '46,000 SY - 46,000 SY'. However, if you zoom in enough, the card shows the correct date range. This mirrors the rendering of the ruler numbers. when zoomed in, the ruler scale shows more granular numbers. but when at intermediate zoom levels, the scale just shows every tick is '46,000'.

7. In Biographical Mode - the 'parent event' (long time scales) do not show tooltips on hover - the mouse incorrectly shows the 'hand' icon, 

## Small changes

c1. the event 'card' should show the time or time ranges in multiple 'epoch time scales' - whichever have their ruler's enabled (This may need to calculate the appropriate numbers from the ruler offsets and scales), 

c2. Increase contrast of time ruler numbers - make them darker

c3. Add a master 'font size' slider to the view config flyout pane

c4. The "show great void' toggle appears to do nothing - I don't think we need this anyway - remove it.

c5. Reduce the size of the event circles (in Geographical mode) to approx. 40% of their current size.

## larger upgrades

u1. geographical 'sub-areas' - so that the lanes can be sub-divided into different horizontal lanes. these will not be drawn on the background of the canvas, but will dictate a more granular 'y-coordinate' for the plotting of the events. That way, events that happen at the same time in the same geographical area can be in 'sub-areas', and so be plotted as separate circles that do not overlap, but look stacked vertically (as long as the events have different sub-areas defined in the data).

u2. Vertical viewport scaling/zooming - be able to independently 'zoom' or scale the lanes - to see more or less lanes in the viewport, and then the canvas vertical  scroll will be better... (the mouse wheel control should zoom both dimensions simultaneously, but then there should be 2 separate slider controls on the header pane for vertical zoom and horizontal zoom - to replace the current zoom buttons)