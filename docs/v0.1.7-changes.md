Add a 'time measer' control - changes the cursor to a cross-hair, click once for the start, and ass the cursor moves, a tooltip card shows the time-span between the start point and the current cursor location.
Then when the user clicks a second time, it makes the measure line and the card perminently visible. note that this does not get added to the persistant data, and is only saved for rendering in the current browser session.