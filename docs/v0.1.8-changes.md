# Change Request v0.1.8 — 

Draw events for the birth and death (or #departure/arrival) of each entity - from the data in the 'entities' collection. these should appear as small markers on the timeline at the position of the year in which the event occurs.

Edit data dialog:
- Cannot see which colour on the colour picker is currently selected
- For the Lanes and Entities lists - add a column showing the colour parameter. Hide the ID column
- For the Events list - hide the ID column, show the Type and Lane columns (as appropriately colour coded pills)
- Time/Epoch Form - not showing the Epcoh Title (not populating the text box)
- There should be another tab/list/form for the Rulers.

Event Tooltip datacard
- Move the participants 'list' back into the left column of the card, and change back to a horizontal flex layout
- move the importance stars to the right column of the card - 2nd row, under the event category, above the dates
- Dates styles are incorrect. the 'primary' date should be black, bold, and slightl larger than the others. ALL 'other' dates should be in the smaller, red italic style.
- if the dates to be shown are date ranges, then the right column of the card should grow to accomodate extra width (50% or card), and the date font should be slightly smaller than for single dates.
